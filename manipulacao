package jdbc.example;

import java.sql.Connection;
import java.sql.SQLException;
import java.util.Scanner;
import java.sql.PreparedStatement;

public class manipulacao {
    public static void main(String[] args) {
        try (Scanner sc = new Scanner(System.in)) {
            System.out.println("Digite um valor para a coluna id_junior:");
            int id = sc.nextInt();

            // Tentativa de obter a conexão e auto fechá-la ao final do bloco try
            try (Connection connection = conexao.getConnection()) {
                // Preparar a consulta SQL para inserção, usando placeholders (?)
                String sql = "INSERT INTO teste (id_junior) VALUES (?)";
                try (PreparedStatement statement = connection.prepareStatement(sql)) {
                    // Substituir o placeholder pelo valor real
                    statement.setInt(1, id); // Define o valor de id_junior

                    // Executar a atualização
                    int linhasAfetadas = statement.executeUpdate();
                    System.out.println("Linhas afetadas: " + linhasAfetadas);
                }
            } catch (SQLException e) {
                e.printStackTrace();
            }
        }
    }
}
